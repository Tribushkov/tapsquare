define([
	'backbone'
], function(
	Backbone
) {

	var Model = Backbone.Model.extend({
		//  стандартные функции:
		initialize: function() {

		},
	}, {
		//  кастомные функции:
	});

	return Model;

});
